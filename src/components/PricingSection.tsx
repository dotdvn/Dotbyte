import { Check, ArrowUpRight } from 'lucide-react';
import { PRICING_PLANS_DOT } from '../data';
export default function PricingSection({onSelectPlan}:{onSelectPlan:(name:string,price:string)=>void}){return <section id="pricing" className="studio-wrap studio-section"><div className="section-index micro"><span>04 / A GOOD PLACE TO START</span><span>STUDENT-FRIENDLY BY DESIGN</span></div><div className="studio-heading"><h2>Big ideas.<br/><span>Grounded prices.</span></h2><p>Transparent starting prices for your next build.<br/>Choose a starting point. We’ll work out the details together.</p></div><div className="plans-grid">{PRICING_PLANS_DOT.map(tier=><article key={tier.id} className={`plan-card ${tier.isPopular?'plan-popular':''}`} id={`pricing_tier_card_${tier.id}`}><div className="plan-heading"><span className="micro">{tier.isPopular?'MOST POPULAR':'CUSTOM BUILD'}</span><h3>{tier.name}</h3><p>{tier.description}</p><strong>{tier.priceLabel}</strong></div><ul>{tier.features.map(feature=><li key={feature}><Check size={14}/><span>{feature}</span></li>)}</ul><button className="studio-button" id={`btn_choose_plan_${tier.id}`} onClick={()=>onSelectPlan(tier.name,tier.priceLabel)}>Choose this plan <ArrowUpRight size={16}/></button></article>)}</div></section>}
